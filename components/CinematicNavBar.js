import NavBarCore from './NavBarCore'

const CinematicNavBar = (props) => (
  <nav className="navbar navbar-expand-lg navbar-dark sticky-top">
    <NavBarCore></NavBarCore>
  </nav>
)

export default CinematicNavBar
